# API Advanced

Scripts that query the Reddit API to parse and present data, covering
pagination, recursive API calls, and sorting results.

## Tasks

* `0-subs.py` - Returns the number of subscribers for a given subreddit (0 if invalid)
